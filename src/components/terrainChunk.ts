import { vec3 } from "gl-matrix";
import Entity from "../entity";
import { Component } from "./component";

export default class TerrainChunk implements Component {
  constructor(offset: vec3) {

  }

  onAdd = (entity: Entity) => {

  }
}